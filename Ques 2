//2. WAP a C program to Convert temperature from Celsius to Fahrenheit using the formula: F = (C9/5) + 32.

//Answer -

//Program:

#include <stdio.h>

int main() {

int celsius;

float fahrenheit;

printf("Enter temperature in Celsius: ");

scanf("%d", &celsius);

// Convert to Fahrenheit

fahrenheit (celsius 9.0/5.0) + 32;

printf("Temperature in Fahrenheit%.2f\n", fahrenheit);

return 0;
}